/**
 * EnumKey.ts
 *
 * This file contains all the keys used for TanStack Query.
 * Using an enum ensures consistency and avoids magic strings throughout the app.
 *
 * @example
 * export enum EnumKey {
 *   EPISODE = "episode",
 *   DEVICE = "device",
 * }
 */
export enum EnumKey {
  EPISODE = "episode",
}
